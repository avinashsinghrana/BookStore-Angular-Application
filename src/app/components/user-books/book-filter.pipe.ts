// import {PipeTransform, Pipe} from '@angular/core';
// import {Book} from 'src/app/models/book.model';
//
// @Pipe({
//   name: 'bookFilter'
// })
// export class BookFilterPipe implements PipeTransform {
//   // transform(books: Book[], searchTerm: string): Book[] {
//   //   if (!books || !searchTerm) {
//   //     return books;
//   //   }
//   //   // let size = books.filter(book =>
//   //   //   book.bookName.toLowerCase().indexOf(searchTerm.toLowerCase()) !== -1).length;
//   //   return books.filter(book =>
//   //     book.bookName.toLowerCase().indexOf(searchTerm.toLowerCase()) !== -1 ||
//   //     book.authorName.toLowerCase().indexOf(searchTerm.toLowerCase()) !== -1);
//   // }
//   // transform(books: Book[], searchTerm: string) {
//   //   if (searchTerm) {
//   //     searchTerm = searchTerm.toLowerCase();
//   //     // tslint:disable-next-line:only-arrow-functions
//   //     return books.filter(function(book: any) {
//   //       return book.bookName.toLowerCase().indexOf(searchTerm) > -1 ||
//   //         book.authorName.toLowerCase().indexOf(searchTerm) > -1;
//   //     });
//   //   }
//   //   return books;
//   // }
// }
